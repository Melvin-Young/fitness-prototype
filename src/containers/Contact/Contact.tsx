import React, { FunctionComponent } from 'react';
import Section, { ISectionProps } from '../../components/Section';

export const Contact: FunctionComponent<ISectionProps> = (props) => (
	<Section id={props.id}>Contact</Section>
);
